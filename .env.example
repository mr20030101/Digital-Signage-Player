# Player Configuration
# The player will auto-detect the API URL based on hostname
# 
# For localhost: Uses http://localhost:8000/api
# For production: Uses same hostname as player
#
# You can also override via URL parameter:
# http://yourplayer.com?api=https://api.yourdomain.com/api
